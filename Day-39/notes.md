# Day 39 - AWS S3 Static Website Hosting

Created the `xfusion-web-179688143` S3 bucket in `us-east-1`, enabled static website hosting with `index.html`, configured public access using a bucket policy, uploaded `/root/index.html`, and verified the website returned HTTP 200 with the expected content.
